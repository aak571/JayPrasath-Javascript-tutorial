// Addition in the any place of a string is called string-interpolation

const jay = 'Jay Prasath '

const str1 = 'is a very good boy !!!'

const height = 5.6

const weight = 100

const str2 = ` he stands ${height} ft`

const str3 = ` and he weighs ${weight} KGs` // string interpolation (back tick `)

console.log(jay + str1 + str2 + str3)